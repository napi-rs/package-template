name: Lint

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - run: |
          ls /usr/local/lib/android/sdk/ndk
          ls -R "${ANDROID_NDK_LATEST_HOME}"
